# MathProofMesh 0.5.1

## 调度器正确性修复

本补丁修复“初始路线全部失败后仍连续深挖旧路线、没有生成新机制，并耗尽最终修订预算”的通用调度缺陷。它不针对某一道题绑定路线数或调用数；所有容量、修补和储备参数均可配置。

### 主要变化

- 覆盖率从 `current_paths / initial_paths` 修正为 `current_paths / max_paths`。
- 将结构审查和最终聚合报告同时接入路径统计，结构失败不再表现为 `structurally_valid=None`。
- 证明步数和候选引理只构成原始草稿进展；高置信度 FAIL、结构失败、计划失败和策略失败会折扣或限制其有效进展。
- 执行级、计划级、策略级和未知失败分别使用可配置修补上限；重复失败进入可配置冷却。
- 当所有已探索路线均被审查为 FAIL、仍有路线容量且预算允许时，强制在本轮保留一个 `widen`。
- `max_actions_per_round`、`widen_paths_per_action`、修补次数、冷却轮次和最终修订周期全部进入 `scheduler` 配置。
- 动作成本按实际拓宽批次、分段续推、Delta Reviewer、Claim 提取、候选验证和 Meta-review 动态估算；预算不足时可缩小拓宽批次。
- 最终储备包含综合与终审，以及配置数量的“修订 + 重新审计”完整周期。
- 调度 JSON 和 Activity 详细模式记录所有候选动作的分数、排名、预计成本、未选原因和预算阻断原因。
- 分段续推产生的新一轮 `ProofAttempt` 使用新的 Attempt ID，避免历史验证报告被同一 ID 覆盖。

## 终审与可修复缺口

- 标准公认定理不再仅因缺少书目链接或页码而被结构门拒绝。
- 该放宽不适用于数学条件：综合器仍必须写出所调用定理的准确形式，并显式验证每个假设和适用条件。
- 缺失定理适用条件被视为可修复的执行级缺口；预算允许时进入定向修订，而不是直接把不完整证明升级为通过。
- 每次最终修订后重新运行独立结构审计和详细审计，不复用修订前的 PASS，也不因已执行修订而自动放行。
- 策略级矛盾不会靠继续润色同一路线掩盖，最终状态保持 `unverified`。
- 增加回归测试，覆盖“缺失适用条件 -> 修订 -> 完整复审 -> PASS”的完整路径，以及标准定理书目要求的边界。

## 故障切换与分段完整性

- Synthesizer 的可重试连接错误现在也会触发跨 key/Agent 接力，不再直接退回本地拼装草稿。
- 故障切换成功后，最终审计排除实际完成综合的 Agent，继续保持作者与审稿人隔离。
- `ProofDelta` 允许后续步骤依赖同一 Delta 中更早的步骤，同时继续拒绝前向依赖、自依赖和缺失依赖。
- 分段策略违规会先生成完整 `VerificationIssue`，再形成 FAIL 报告，避免 Pydantic 校验错误掩盖真实失败原因。

## DeepSeek 预置配置

- 冒烟版：每个 Agent 与每个证明分段请求的输出上限均为 50,000 tokens；每段最多新增 8 个结构化步骤；总调用上限为 28，并预留一次最终修订和复审。
- 正式版：每个 Agent 与每个证明分段请求的输出上限均为 100,000 tokens；每段最多新增 12 个结构化步骤；总调用上限为 42，最多执行三次最终修订。
- “8 步/12 步”是单个 `ProofDelta` 的新增结构化步骤上限，不是整道题的总证明步数或总模型调用数。

## 兼容性

- 不改变 DeepSeek SSE、检查点恢复和跨 key 接力协议。
- 旧配置没有 `scheduler` 段时使用安全默认值。
- 通用配置默认仍可关闭 continuation；正式 DeepSeek 配置继续启用 V4 Pro、`reasoning_effort=max` 和 SSE。
- 模型或 API 服务自身若有更低的输出硬限制，仍以提供商返回为准。

## 验证

- `python -m pytest -q`：60 passed, 1 skipped。
- `python -m ruff check .`：PASS。
- `python -m ruff format --check .`：PASS。
- `python -m compileall -q src`：PASS。
- 未在自动化构建中使用真实 DeepSeek 凭据或发出付费请求。
