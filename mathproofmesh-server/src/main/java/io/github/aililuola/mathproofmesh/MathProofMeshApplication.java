package io.github.aililuola.mathproofmesh;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Spring Boot process entry point for the secured HTTP and operational adapters. */
@SpringBootApplication
public class MathProofMeshApplication {
    public static void main(String[] args) {
        SpringApplication.run(MathProofMeshApplication.class, args);
    }
}
