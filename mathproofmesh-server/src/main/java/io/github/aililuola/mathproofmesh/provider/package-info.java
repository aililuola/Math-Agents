/**
 * Bounded JDK HTTP/SSE provider adapters. Provider reasoning is summarized by
 * size and digest and is never exposed as an inter-agent message.
 */
package io.github.aililuola.mathproofmesh.provider;
