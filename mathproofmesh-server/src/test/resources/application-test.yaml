spring:
  datasource:
    url: ${TEST_POSTGRES_JDBC_URL}
    username: ${TEST_POSTGRES_USERNAME}
    password: ${TEST_POSTGRES_PASSWORD}
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: true
