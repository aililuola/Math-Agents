package io.github.aililuola.mathproofmesh.strategydiversity;

/** Bounded representation vocabulary; raw model prose is never hashed as a representation. */
public enum StructuredRepresentationKind {
  GRAPH,
  FINITE_SET,
  SEQUENCE,
  NUMBER_THEORETIC,
  LINEAR_ALGEBRA,
  ALGEBRAIC,
  GEOMETRIC,
  PROBABILISTIC,
  ABSTRACT
}
